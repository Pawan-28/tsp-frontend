# Frontend — copy to .env.local for local dev

# Development: defaults to http://localhost:5000 (see api.js — Nitro breaks Vite /api proxy).
# If local MySQL is down, point at Hostinger:

VITE_API_URL=http://localhost:3500
